name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install


      - name: SonarCloud Scan
        uses: sonarsource/sonarcloud-github-action@v1
        with:
          organization: 3-2-project-org
          token: ${{ secrets.SONAR_TOKEN }}
          projectKey: 3-2-project-org_CTSE-_Assignment_2


      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}


      - name: Build docker image
        run: docker build -t rexhonz77/products-microservice .
        
      - name: Publish image to docker hub
        run: docker push rexhonz77/products-microservice:latest


      



  deploy:
    needs: build
    runs-on: self-hosted
    steps:
      - name: Pull image from docker hub
        run: docker pull rexhonz77/products-microservice:latest
      - name: Delete old container
        run: docker rm -f products-microservice-container || true
      - name: Run docker container
        run: docker run -d -p 3000:3001 --name products-microservice-container rexhonz77/products-microservice:latest
   
